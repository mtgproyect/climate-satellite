name: Validar esqueleto

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validar manifiesto
        run: python -m json.tool docs/manifiesto.json > /dev/null
